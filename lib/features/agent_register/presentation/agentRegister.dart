import 'package:flutter/material.dart';

class AgentRegister extends StatelessWidget {
  const AgentRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Agent Register"),
      ),
      body: Row(
        children: [Text('Agent Register')],
      ),
    );
  }
}
